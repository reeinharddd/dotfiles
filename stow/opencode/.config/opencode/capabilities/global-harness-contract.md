# Global Harness Contract (pointer)

> Canonical content lives in `instructions/00-global-contract.md` (always loaded via the
> `instructions` key in `opencode.jsonc`). This file is the on-demand documentation pointer only —
> do not maintain a second copy of the rules here.

Read the canonical file for: Identity, Veracidad, Seguridad, Contexto, Memoria, Herramientas,
Routing, Completion, and conflict precedence (GLOBAL > PROJECT > SKILL > EXTERNAL).
