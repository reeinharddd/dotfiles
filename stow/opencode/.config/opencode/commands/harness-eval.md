---
description: Evaluate deterministic global harness invariants for the current project.
---

Run:

```bash
~/.config/opencode/scripts/opencode-harness-eval "$PWD"
```

This tests configuration, capability health, project contract, CodeGraph, and task state without
calling a model.
