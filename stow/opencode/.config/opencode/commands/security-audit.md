---
description: Run a read-only security audit for the current project.
---

Run:

```bash
~/.config/opencode/scripts/opencode-security-audit "$PWD"
```

Review every finding. The audit does not install scanners or modify source.
