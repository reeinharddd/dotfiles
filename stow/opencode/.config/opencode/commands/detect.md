---
name: detect
description: "Detecta el stack tecnológico del proyecto actual (lenguaje, framework, tools, skills a cargar)."
---
Run `scripts/detect.sh` from project-auto-detect skill and show results.

Load the project-auto-detect skill, run detection, then:
1. Read project AGENTS.md if exists
2. Match skills to detected stack
3. Report session context
