---
description: Run the read-only Ubuntu workstation health audit
---

# System Audit

Run `~/.config/opencode/scripts/ubuntu-health-audit.sh`.

This audit reports package updates, failed services, runtime drift, security status, storage, and
Mise-managed tools. It does not install packages, change services, modify firewall rules, or delete
state.
