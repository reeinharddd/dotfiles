#!/usr/bin/env bash
set -euo pipefail
root="$(git rev-parse --show-toplevel)"
command -v gitleaks >/dev/null || { echo "gitleaks no está instalado"; exit 1; }
gitleaks git --staged --redact --no-banner --config "$root/.gitleaks.toml" "$root"